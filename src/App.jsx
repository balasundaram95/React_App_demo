// src/App.jsx

import React from 'react';
import AppRouter from './routes/AppRouter';

function App() {
  // The App component now just sets up the router for the entire application.
  return <AppRouter />;
}

export default App;